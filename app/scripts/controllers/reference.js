'use strict';

/**
 * @ngdoc function
 * @name pacgApp.controller:ReferenceCtrl
 * @description
 * # ReferenceCtrl
 * Controller of the pacgApp
 */
angular.module('pacgApp')
  .controller('ReferenceCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
