'use strict';

/**
 * @ngdoc function
 * @name pacgApp.controller:SummonCtrl
 * @description
 * # SummonCtrl
 * Controller of the pacgApp
 */
angular.module('pacgApp')
  .controller('SummonCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
